import { Ch5BaseClassForCli } from "../Ch5BaseClassForCli";
import { ICh5Cli } from "../ICh5Cli";
export declare class Ch5ExportProjectCli extends Ch5BaseClassForCli implements ICh5Cli {
    showOutputMessages: boolean;
    constructor(showOutputMessages?: boolean);
    /**
     * Method for exporting project
     */
    run(): Promise<any>;
    /**
     * Copy Files
     * @param {*} folderPathActual
     * @param {*} excludedFiles
     * @param {*} includedFiles
     * @param {*} completeFileName
     */
    copyFiles(folderPathActual: string, excludedFiles: string[], includedFiles: string[], completeFileName: string): Promise<any>;
    /**
     * Zip files
     * @param {*} folderPathActual
     * @param {*} completeFileName
     */
    zipFiles(folderPathActual: any, completeFileName: string): Promise<any>;
    /**
     * Create Temporary Folder for copy files before zipping
     */
    createTempFolder(folderPathName: string): string;
}
