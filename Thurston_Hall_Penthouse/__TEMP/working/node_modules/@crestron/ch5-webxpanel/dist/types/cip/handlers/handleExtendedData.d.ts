import CIPPacketHandler from "../CIPPacketHandler";
/**
 * Handles the reception of an EXTENDED DATA packet (2 bytes for cresnet length)
 * @param packet <0x12> <length> <length> <handler> <handler> <data> ... <data>
 */
export default function handleExtendedData(this: CIPPacketHandler, packet: Uint8Array): void;
