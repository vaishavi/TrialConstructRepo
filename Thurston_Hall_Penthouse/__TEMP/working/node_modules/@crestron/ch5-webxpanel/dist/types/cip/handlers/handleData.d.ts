import CIPPacketHandler from "../CIPPacketHandler";
/**
 * Handles the reception of a DATA packet (1 byte for cresnet length)
 * @param packet <0x05> <length> <length> <handler> <handler> <data> ... <data>
 */
export default function handleData(this: CIPPacketHandler, packet: Uint8Array): void;
