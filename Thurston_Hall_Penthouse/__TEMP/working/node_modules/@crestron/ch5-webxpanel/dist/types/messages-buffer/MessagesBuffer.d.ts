export default class MessagesBuffer<T> {
    private _items;
    private _isProcessing;
    constructor();
    size(): number;
    isEmpty(): boolean;
    push(element: T): void;
    pop(): T | undefined;
    startProcessing(): void;
    stopProcessing(shouldClearBuffer?: boolean): void;
    get isProcessing(): boolean;
    private clear;
}
