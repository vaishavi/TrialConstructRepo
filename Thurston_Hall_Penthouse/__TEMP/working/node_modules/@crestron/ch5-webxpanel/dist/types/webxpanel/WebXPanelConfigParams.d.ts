interface ExternalConfig {
    host: string;
    ipId: string;
    port: number;
    roomId: string;
    tokenUrl: string;
    tokenSource: string;
    authToken: string;
}
interface InternalConfig {
    url: string;
    port: number;
    roomId: string;
    tokenUrl: string;
    tokenSource: string;
}
declare const defaultConfig: ExternalConfig;
export { InternalConfig, ExternalConfig, defaultConfig };
