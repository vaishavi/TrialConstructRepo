import CIPPacketHandler from "../CIPPacketHandler";
/**
 * Handles the reception of the AUTHENTICATE_RESPONSE packet
 * @param packet <0x0C> <0x00> <0x03> <Handler> <Handler> <Access Level>
 */
export default function handleAuthenticateResponse(this: CIPPacketHandler, packet: Uint8Array): void;
