declare class WebXPanelConfig {
    private static _instance;
    private _url;
    private _ipId;
    private _roomId;
    private _jwt;
    private _tokenSource;
    private _tokenUrl;
    private _authToken;
    private constructor();
    /**
     * Overwrites the current configuration with the one provided as a parameter
     * @param config
     */
    setParameters(config?: {
        url: URL;
        ipId: string;
        roomId: string;
        tokenSource: string;
        tokenUrl: string;
        authToken: string;
    }): void;
    static getInstance(): WebXPanelConfig;
    get url(): string;
    get ipId(): number;
    get roomId(): string;
    get jwt(): string;
    set jwt(jwt: string);
    get tokenSource(): string;
    get tokenUrl(): string;
    get secureToken(): string;
}
declare const WebXPanelConfigInstance: WebXPanelConfig;
export { WebXPanelConfig, WebXPanelConfigInstance };
