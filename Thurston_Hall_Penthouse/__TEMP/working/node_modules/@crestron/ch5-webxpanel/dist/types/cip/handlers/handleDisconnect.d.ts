import CIPPacketHandler from "../CIPPacketHandler";
/**
 * Handles the DISCONNECT packet
 * @param packet
 *    <0x03> <0x00> <0x02> <Handler> <Handler>
 *    OR
 *    <0x03> <0x00> <0x03> <Handler> <Handler> <ReasonCode>
 */
export default function handleDisconnect(this: CIPPacketHandler, packet: Uint8Array): void;
